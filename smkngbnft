# inputs
packs_per_day = float(input('Average number of sigarette packs per day: '))
time_to_smoke = int(input('Average time to smoke one sigarette (est. 10 min): '))
price = int(input('Sigarette pack\'s price: '))
# constants
ITEMS_IN_PACK = 20
MINUTES_IN_HOUR = 60
DAYS_IN_MTH = 30
WORK_DAY = 8
# calculations
expence = DAYS_IN_MTH * price * packs_per_day
working_days = ITEMS_IN_PACK * time_to_smoke * packs_per_day * DAYS_IN_MTH / MINUTES_IN_HOUR / WORK_DAY
print('Total benefit: ' + str(round(working_days)) + ' full 8-hour days + '+ str(expence) + ' per mth')
